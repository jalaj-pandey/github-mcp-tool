[project]
name = "github-tool"
version = "0.1.0"
description = "This is a tool to interact with GitHub API"
authors = [
    { name = "Jalaj Pandey" }]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]
